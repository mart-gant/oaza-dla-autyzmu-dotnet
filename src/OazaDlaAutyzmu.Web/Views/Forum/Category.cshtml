@model List<OazaDlaAutyzmu.Application.DTOs.ForumTopicDto>
@{
    ViewData["Title"] = ViewBag.CategoryName;
}

<div class="max-w-7xl mx-auto px-4 py-8">
    <nav class="mb-6 text-sm text-gray-600 dark:text-gray-400">
        <a asp-action="Index" class="link-strong">Forum</a>
        <span class="mx-2">/</span>
        <span class="text-gray-900 dark:text-white">@ViewBag.CategoryName</span>
    </nav>

    <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold text-gray-900 dark:text-white">@ViewBag.CategoryName</h1>
        
        @if (User.Identity?.IsAuthenticated == true)
        {
            <a asp-action="CreateTopic" asp-route-categoryId="@ViewBag.CategoryId" 
               class="px-6 py-2 btn-primary rounded-lg transition">
                Nowy temat
            </a>
        }
    </div>

    @if (Model.Count == 0)
    {
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md p-8 text-center">
            <p class="text-gray-600 dark:text-gray-400 mb-4">Brak tematów w tej kategorii.</p>
            @if (User.Identity?.IsAuthenticated == true)
            {
                <a asp-action="CreateTopic" asp-route-categoryId="@ViewBag.CategoryId" 
                   class="inline-block px-6 py-2 btn-primary rounded-lg transition">
                    Utwórz pierwszy temat
                </a>
            }
        </div>
    }
    else
    {
        <div class="space-y-4">
            @foreach (var topic in Model)
            {
                <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md p-6 hover:shadow-lg transition">
                    <div class="flex justify-between items-start">
                        <div class="flex-1">
                            <div class="flex items-center gap-2 mb-2">
                                @if (topic.IsPinned)
                                {
                                    <span class="px-2 py-1 bg-yellow-100 text-yellow-800 text-xs font-semibold rounded">
                                        📌 Przypięty
                                    </span>
                                }
                                @if (topic.IsLocked)
                                {
                                    <span class="px-2 py-1 bg-gray-100 text-gray-800 text-xs font-semibold rounded">
                                        🔒 Zamknięty
                                    </span>
                                }
                            </div>

                            <a asp-action="Topic" asp-route-id="@topic.Id" 
                                         class="text-xl font-bold link-strong">
                                @topic.Title
                            </a>
                            
                            <div class="mt-2 text-sm text-gray-600 dark:text-gray-400">
                                <span>przez <span class="font-medium">@topic.UserName</span></span>
                                <span class="mx-2">•</span>
                                <span>@topic.CreatedAt.ToString("dd.MM.yyyy HH:mm")</span>
                            </div>

                            @if (topic.LatestPost != null && topic.PostCount > 1)
                            {
                                <div class="mt-3 text-sm text-gray-500 dark:text-gray-400">
                                    <span class="font-medium">Ostatnia odpowiedź:</span>
                                    <span class="ml-1">@topic.LatestPost.UserName</span>
                                    <span class="mx-2">•</span>
                                    <span>@topic.LatestPost.CreatedAt.ToString("dd.MM.yyyy HH:mm")</span>
                                </div>
                            }
                        </div>
                        
                        <div class="text-right ml-6">
                            <div class="text-sm text-gray-600 dark:text-gray-400">
                                <div class="font-bold text-gray-900 dark:text-white">@topic.PostCount</div>
                                <div>odpowiedzi</div>
                            </div>
                            <div class="text-sm text-gray-500 dark:text-gray-400 mt-2">
                                @topic.ViewCount wyświetleń
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>
